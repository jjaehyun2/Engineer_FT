# The std module contains things that are core / central to the langauge.
import std;

# The `std.io` module.
# This import brings `io` in scope from the `std` module.
import std.io;

# The os module contains os-independent functions that operate on normally
# os-dependent primitives (see python's os mdoule).
import os;