package  {	import com.dobuki.ad.AdThingy;	import flash.events.*;	public class DobukiAdThingy extends AdThingy {		public function DobukiAdThingy() {			addEventListener(Event.ENTER_FRAME,onFrame);			playGroup.playButton.addEventListener(MouseEvent.CLICK,onPlay);			playGroup.visible = false;		}				private function onPlay(e:MouseEvent):void {			var mc:MovieClip = root as MovieClip;			mc.play();		}		private function onFrame(e:Event):void {			var mc:MovieClip = root as MovieClip;			if(!root || mc.loaderInfo.bytesLoaded>=mc.loaderInfo.bytesTotal) {				playGroup.visible = true;				loadingBar.visible = false;				e.currentTarget.removeEventListener(e.type,arguments.callee);			}			else if(mc.framesLoaded>=1+mc.scenes[0].numFrames+mc.scenes[1].numFrames) {				playGroup.visible = true;			}		}	}	}