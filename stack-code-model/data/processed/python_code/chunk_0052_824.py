package {	import flash.display.*;	import flash.events.*;	public class SnowFlake extends MovieClip {				var radians = 0;		var speed = 0;		var radius = 5;		var stageHeight;				public function SnowFlake(h:Number)		{			speed = .01+.2*Math.random();			radius = .1+.2*Math.random();						stageHeight = h;						this.addEventListener(Event.ENTER_FRAME, Snowing);		}				function Snowing(e:Event)		{			radians += speed;						this.x += Math.round(Math.cos(radians))/2;			this.y -= 1;						if (this.y < -200)			//if (this.y < 100)			{				this.y = 1000;			}		}	}}