/****************************	Background Control *** *************************//* Notes:** This class controls manages the stage background*/package com{    // Import flash classes    import flash.display.Sprite;    import flash.display.Graphics;	    // Extend a sprite since we don't need multiple frames    public class BackgroundControl extends Sprite{        // Class variables        private var colour:uint;        // Constructor        public function BackgroundControl(color:uint):void{            this.colour = color;        }  		// A function to draw the stage background.. in this case, a large black rectangle..        public function drawBackground():void{            graphics.beginFill(colour);            graphics.drawRect(0, 0, stage.stageWidth, stage.stageHeight);            graphics.endFill();        }    }}